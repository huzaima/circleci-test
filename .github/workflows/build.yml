
on:
    push:
        paths-ignore:
            - "ops/**"
            - "**.md"
            - "docs/**"
            - "CODEOWNERS"
        branches-ignore:
          - 'refs/tags/*'
    release:
        types:
          - published
jobs:
    build:
        runs-on: ubuntu-latest
        steps:
            - run: |
								if [[ ${{ github.event_name }} == 'release' ]]; then
									echo "HEROKU_APP_NAME=${{ secrets.HEROKU_PRODUCTION_APP_NAME }}" >> $GITHUB_ENV
								else
									echo "HEROKU_APP_NAME=${{ secrets.HEROKU_STAGING_APP_NAME }}" >> $GITHUB_ENV
								fi
                # [[ ${{ github.event_name}} == 'release' ]] && 
                # [[ ${{ github.event_name}} != 'release' ]] && echo "HEROKU_APP_NAME=${{ secrets.HEROKU_STAGING_APP_NAME }}" >> $GITHUB_ENV
